db:
  environment:
    - MONGO_DATA_DIR=/data/db
    - MONGO_LOG_DIR=/dev/null
  image: 'mongo:3.6.3-jessie'
  ports:
    - '27017:27017'
  restart: always
web:
  image: 'python:2.7-alpine'
  build: ./web
  links:
    - db
  ports:
    - '5000:5000'
  volumes:
    - ./web:/code
